/*
separate file since linting should enabled for prisma config file and
tsconfig.build.json can only include ts files under rootDir (./src)
*/
{
  "extends": "../tsconfig.base.json",
  "include": ["src/**/*.ts", "prisma.config.ts", "prisma/seed.ts"]
}
